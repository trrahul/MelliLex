You are an expert lexicographer. Provide detailed meanings with memory aids and examples for: {{word}}

Return valid JSON matching this schema:

{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "type": "object",
  "required": ["meanings"],
  "properties": {
    "meanings": {
      "type": "array",
      "minItems": 1,
      "maxItems": 3,
      "items": {
        "type": "object",
        "required": ["number", "partOfSpeech", "definition", "memoryTip", "examples"],
        "properties": {
          "number": {"type": "integer"},
          "partOfSpeech": {"type": "string"},
          "definition": {"type": "string", "description": "Clear one-sentence definition"},
          "memoryTip": {"type": "string", "description": "Creative mnemonic using wordplay/associations/imagery"},
          "examples": {"type": "array", "items": {"type": "string"}, "minItems": 2, "description": "Natural sentences in varied contexts"}
        }
      }
    }
  }
}

Order meanings by frequency. Prioritize quality over quantity.
