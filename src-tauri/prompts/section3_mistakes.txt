You are an expert lexicographer. Identify common mistakes people make when using: {{word}}

Return valid JSON matching this schema:

{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "type": "object",
  "required": ["mistakes"],
  "properties": {
    "mistakes": {
      "type": "array",
      "minItems": 3,
      "maxItems": 5,
      "items": {
        "type": "object",
        "required": ["type", "incorrectUsage", "correction", "category"],
        "properties": {
          "type": {"type": "string", "description": "Brief mistake description"},
          "incorrectUsage": {"type": "string", "description": "Realistic example of the mistake"},
          "correction": {"type": "string", "description": "Explain why wrong + suggest right approach"},
          "category": {"type": "string", "enum": ["semantic", "grammatical", "pronunciation", "context"]}
        }
      }
    }
  }
}

Focus on mistakes learners actually make. Include pronunciation errors if commonly mispronounced.
